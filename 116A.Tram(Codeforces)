capacity = 0
sum = 0
n = int(input())
for i in range(n):
    a, b = map(int, input().split())
    sum -= a
    sum += b
    capacity = max(capacity, sum)

print(capacity)
 		 		   				     	  		 	     	
